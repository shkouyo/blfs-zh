<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE part PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="postlfs-config-vimrc" xreflabel="vimrc 文件">
  <?dbhtml filename="vimrc.html"?>


  <title>/etc/vimrc 和 ~/.vimrc 文件</title>

  <indexterm zone="postlfs-config-vimrc">
    <primary sortas="e-etc-vimrc">/etc/vimrc</primary>
  </indexterm>

  <indexterm zone="postlfs-config-vimrc">
    <primary sortas="e-AA.vimrc">~/.vimrc</primary>
  </indexterm>

  <para>
    LFS 手册将 <application>Vim</application> 安装为其文本编辑器。此时应注意，存在 <emphasis>很多</emphasis> 不同的编辑应用程序，包括 <application>Emacs</application>、<application>nano</application>、<application>Joe</application> 等等。任何在网上（尤其是新闻组）待过一段时间的人，肯定至少见过一次争论，通常涉及 <application>Vim</application> 和 <application>Emacs</application> 用户！
  </para>

  <para>
    LFS 手册创建了一个基本的 <filename>vimrc</filename> 文件。在本节中，我们将尝试增强此文件。启动时，<command>vim</command> 会读取全局配置文件（<filename>/etc/vimrc</filename>）以及用户特定的文件（<filename>~/.vimrc</filename>）。可以调整其中一个或两者，以适应您特定系统的需求。
  </para>

  <para>
    以下是一个稍微扩展的 <filename>.vimrc</filename>，您可以将其放入 <filename>~/.vimrc</filename> 中以提供用户特定的效果。当然，如果您将其放入 <filename>/etc/skel/.vimrc</filename>，则以后添加到系统的用户都将获得它。您也可以将文件从 <filename>/etc/skel/.vimrc</filename> 复制到系统上已有用户（如 <systemitem class='username'>root</systemitem>）的家目录中。如果您直接从 <filename class="directory">/etc/skel</filename> 复制任何内容，请务必设置权限、所有者和组。
  </para>

<screen><literal>" Begin .vimrc

set columns=80
set wrapmargin=8
set ruler

" End .vimrc</literal></screen>

  <para>
    请注意，注释标记是 " 而不是更常见的 # 或 //。这是正确的，<filename>vimrc</filename> 的语法略有不同。
  </para>

  <para>
    以下是对此示例文件中每个选项含义的快速说明：
  </para>

  <itemizedlist>
    <!--
    <listitem>
      <para>
        <option>set nocompatible</option> : This option
        stops <command>vim</command> from behaving in a strongly <command>vi
        </command>-compatible way. It should be at the start of any
        <filename>vimrc </filename> file as it can affect lots of other
        options which you may want to override.
      </para>
    </listitem>
    <listitem>
      <para>
        <option>set bs=2</option>: This influences the behavior of the
        backspace option.  It is fairly complex so see <command>:help 'bs'
        </command> for more details.
      </para>
    </listitem>
    -->
    <listitem>
      <para>
        <option>set columns=80</option>：这只是设置屏幕上使用的列数。
      </para>
    </listitem>
    <!--
    <listitem>
      <para>
        <option>set background=dark</option>: This tells
        <command>vim</command> to use colors which look good on a dark
        background.
      </para>
    </listitem>
    -->
    <listitem>
      <para>
        <option>set wrapmargin=8</option>：这是从右窗口边框开始换行的字符数。
      </para>
    </listitem>
    <!--
    <listitem>
      <para>
        <option>syntax on</option>: Enables
        <command>vim</command>'s syntax highlighting.
      </para>
    </listitem>
    -->
    <listitem>
      <para>
        <option>set ruler</option>：这会使 <command>vim</command> 在屏幕右下角显示当前行号和列号。
      </para>
    </listitem>
  </itemizedlist>

  <para>
    关于 <command>vim</command> <emphasis>众多</emphasis> 选项的更多信息，可以在 <command>vim</command> 内部阅读帮助。在 <command>vim</command> 中键入 <command>:</command><option>help</option> 获取通用帮助，或键入 <command>:</command><option>help usr_toc.txt</option> 查看用户手册目录。
  </para>

</sect1>
