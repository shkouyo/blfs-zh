<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="postlfs-config-skel" xreflabel="为添加用户进行配置">
  <?dbhtml filename="skel.html"?>


  <title>为添加用户进行配置</title>

  <indexterm zone="postlfs-config-skel">
    <primary sortas="e-etc-skel">/etc/skel/*</primary>
  </indexterm>

  <indexterm zone="postlfs-config-skel">
    <primary sortas="e-etc-default-useradd">/etc/default/useradd</primary>
  </indexterm>

  <para>
    <command>/usr/sbin/useradd</command> 命令和 <filename class="directory">/etc/skel</filename> 目录（两者都易于设置和使用）共同提供了一种方法，确保新用户添加到您的 LFS 系统时，在 <envar>PATH</envar>、键盘处理和其他环境变量等方面具有相同的初始设置。使用这两种工具可以更轻松地确保系统中每个新添加的用户都具有这种初始状态。
  </para>

  <para>
    <filename class="directory">/etc/skel</filename> 目录保存了各种初始化文件和其他文件的副本，当 <command>/usr/sbin/useradd</command> 程序添加新用户时，这些文件可能会被复制到新用户的主目录中。
  </para>

  <bridgehead renderas="sect5">Useradd</bridgehead>

  <para>
    <command>useradd</command> 程序使用保存在 <filename>/etc/default/useradd</filename> 中的一组默认值。该文件由 <application>Shadow</application> 包在基础 LFS 安装中创建。如果它已被删除或重命名，<command>useradd</command> 程序会使用一些内部默认值。您可以通过运行 <command>/usr/sbin/useradd -D</command> 来查看默认值。
  </para>

  <para>
    要更改这些值，只需以 <systemitem class='username'>root</systemitem> 用户身份修改 <filename>/etc/default/useradd</filename> 文件。另一种替代直接修改文件的方法是以 <systemitem class='username'>root</systemitem> 用户身份运行 <command>useradd</command>，并在命令行中提供所需的修改。有关如何执行此操作的信息，可以在 <command>useradd</command> 手册页中找到。
  </para>

  <bridgehead renderas="sect5">/etc/skel</bridgehead>

  <para>
    首先，创建一个 <filename class="directory">/etc/skel</filename> 目录，并确保它仅对系统管理员（通常是 <systemitem class="username">root</systemitem>）可写。以 <systemitem class="username">root</systemitem> 身份创建该目录是最佳做法。
  </para>

  <para>
    您从本书此部分放入 <filename class="directory">/etc/skel</filename> 的任何文件的模式应仅对所有者可写。此外，由于无法预知用户最终可能会在其副本中放置何种敏感信息，您应使其对“组”和“其他”不可读。
  </para>

  <para>
    您也可以将其他文件放入 <filename class="directory">/etc/skel</filename>，它们可能需要不同的权限。
  </para>

  <para>
    决定应在每个（或大多数）新用户的主目录中提供哪些初始化文件。您的决定将影响您在接下来的两节（<xref linkend="postlfs-config-profile"/> 和 <xref linkend="postlfs-config-vimrc"/>）中的操作。这些文件中的部分或全部将对 <systemitem class="username">root</systemitem>、任何已有用户和新用户有用。
  </para>

  <para>
    您可能想要放入 <filename class="directory">/etc/skel</filename> 的那些章节中的文件包括 <filename>.inputrc</filename>、<filename>.bash_profile</filename>、<filename>.bashrc</filename>、<filename>.bash_logout</filename>、<filename>.dircolors</filename> 和 <filename>.vimrc</filename>。如果您不确定其中哪些应放在那里，只需继续阅读以下各节，阅读每节及其提供的任何参考资料，然后做出决定。
  </para>

  <para>
    对于放入 <filename class="directory">/etc/skel</filename> 的文件，您将运行一组略有修改的命令。每节都会提醒您这一点。简而言之，本书中的命令是为 <emphasis>不</emphasis> 添加到 <filename class="directory">/etc/skel</filename> 的文件编写的，而是直接将结果发送到用户的主目录。如果文件将放入 <filename class="directory">/etc/skel</filename>，请修改本书的命令以将输出发送到那里，然后将文件从 <filename class="directory">/etc/skel</filename> 复制到相应的目录，例如 <filename class="directory">/etc</filename>、<filename class="directory">~</filename> 或系统中任何其他已有用户的主目录。
  </para>

  <bridgehead renderas="sect5">添加用户时</bridgehead>

  <para>
    当使用 <command>useradd</command> 添加新用户时，请使用 <option>-m</option> 参数，该参数告诉 <command>useradd</command> 创建用户的主目录，并将文件从 <filename class="directory">/etc/skel</filename>（可覆盖）复制到新用户的主目录。例如（以 <systemitem class="username">root</systemitem> 用户身份执行）：
  </para>

<screen role="root"><userinput>useradd -m <replaceable>&lt;新用户&gt;</replaceable></userinput></screen>

  <para>
    如果您与其他 Linux 发行版（例如用于构建 LFS 的主机发行版）共享 <filename class="directory">/home</filename> 或 <filename class="directory">/usr/src</filename>，您可以创建具有相同 UID（以及相同的主组 GID）的用户，以保持文件所有权在各个系统间一致。首先，在 <emphasis>其他发行版</emphasis> 上，获取该用户的 UID 和用户主组的 GID：
  </para>

<screen role="nodump"><userinput>getent passwd <replaceable>&lt;用户名&gt;</replaceable> | cut -d ':' -f 3,4</userinput></screen>

  <para>
    该命令应输出 UID 和 GID，以冒号分隔。现在，在 BLFS 系统上，创建主组和用户：
  </para>

<screen role="root"><userinput>groupadd -g <replaceable>&lt;GID&gt;</replaceable> <replaceable>&lt;用户名&gt;</replaceable> &amp;&amp;
useradd -u <replaceable>&lt;UID&gt;</replaceable> -g <replaceable>&lt;用户名&gt;</replaceable> <replaceable>&lt;用户名&gt;</replaceable></userinput></screen>

</sect1>
