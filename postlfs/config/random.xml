<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="postlfs-config-random" xreflabel="随机数生成" revision="sysv">
  <?dbhtml filename="random.html"?>


  <title>随机数生成</title>

  <indexterm zone="postlfs-config-random">
    <primary sortas="f-random">random</primary>
  </indexterm>

  <para>
    Linux 内核提供了一个随机数生成器，可通过 <filename class="devicefile">/dev/random</filename> 和 <filename class="devicefile">/dev/urandom</filename> 访问。使用 random 和 urandom 设备的程序（如 <application>OpenSSH</application>）将受益于这些说明。
  </para>

  <para>
    当 Linux 系统在缺少大量用户交互的情况下启动时，熵池（用于计算随机数的数据）可能处于相当可预测的状态。这带来了一个现实的可能性，即启动时生成的数字可能总是相同的。为了抵消这种影响，您应该在关机与启动之间保存熵池信息。
  </para>

  <para>
    安装 <xref linkend="bootscripts"/> 软件包中包含的 <filename>/etc/rc.d/init.d/random</filename> 初始化脚本。
  </para>

<screen role="root"><userinput>make install-random</userinput></screen>

</sect1>
