<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="advanced-network" revision="systemd">
  <?dbhtml filename="advanced-network.html"?>


  <title>高级网络设置</title>

  <sect2 id="network-bridge">
    <title>网络桥接</title>

    <sect3 role="kernel" id="advanced-network-bridge-kernel">
      <title>内核配置</title>

      <para>
        在内核配置中启用以下选项，如有必要则重新编译内核：
      </para>

<screen><literal>Networking support: Y
  Networking options:
    802.1d Ethernet Bridging: M or Y</literal></screen>

      <indexterm zone="advanced-network advanced-network-bridge-kernel">
        <primary sortas="d-advanced-network-bridge">高级网络桥接</primary>
      </indexterm>

    </sect3>

    <sect3>
      <title>设置网络桥接</title>

      <para>
        本节将讨论如何使用 <command>systemd-networkd</command> 设置网络桥接。在以下示例中，<replaceable>eth0</replaceable> 表示被桥接的外部接口，而 <replaceable>br0</replaceable> 表示桥接接口。
      </para>

      <para>
        要创建桥接接口，请以 <systemitem class="username">root</systemitem> 用户身份运行以下命令来创建配置文件：
      </para>

<screen role="root"><userinput>cat &gt; /etc/systemd/network/50-br0.netdev &lt;&lt; EOF
<literal>[NetDev]
Name=<replaceable>br0</replaceable>
Kind=bridge</literal>
EOF</userinput></screen>

      <para>
        要将网络接口分配给桥接，请以 <systemitem class="username">root</systemitem> 用户身份运行以下命令来创建配置文件：
      </para>

<screen role="root"><userinput>cat &gt; /etc/systemd/network/51-eth0.network &lt;&lt; EOF
<literal>[Match]
Name=<replaceable>eth0</replaceable>

[Network]
Bridge=<replaceable>br0</replaceable></literal>
EOF</userinput></screen>

      <para>
        对需要桥接的其他任何接口重复此过程。请注意，务必不要让任何程序为被桥接的接口分配地址。如果您正在使用 <xref linkend="NetworkManager"/>，<!--or <xref linkend="wicd"/>i,--> 请确保将其配置为忽略被桥接的接口，以及桥接接口本身。
      </para>

      <para>
        如果您所在网络使用 DHCP 分配 IP 地址，请以 <systemitem class="username">root</systemitem> 用户身份运行以下命令来创建配置文件：
      </para>

<screen role="root"><userinput>cat &gt; /etc/systemd/network/60-br0.network &lt;&lt; EOF
<literal>[Match]
Name=<replaceable>br0</replaceable>

[Network]
DHCP=yes</literal>
EOF</userinput></screen>

      <para>
        或者，如果使用静态 IP 设置，请以 &root; 用户身份运行以下命令来创建配置文件：
      </para>

<screen role="root"><userinput>cat &gt; /etc/systemd/network/60-br0.network &lt;&lt; EOF
<literal>[Match]
Name=<replaceable>br0</replaceable>

[Network]
Address=192.168.0.2/24
Gateway=192.168.0.1
DNS=192.168.0.1</literal>
EOF</userinput></screen>

      <para>
        如果您希望与连接到桥接的其他系统通信，例如 qemu 实例，则还需要启用 IP 转发。同样以 &root; 用户身份执行：
      </para>

<screen role="root"><userinput>echo 'IPForward=ipv4' >> /etc/systemd/network/60-br0.network</userinput></screen>

      <para>
        要启用桥接接口，只需以 <systemitem class="username">root</systemitem> 用户身份运行以下命令重启 <command>systemd-networkd</command> 守护进程：
      </para>

<screen role="root"><userinput>systemctl restart systemd-networkd</userinput></screen>

    </sect3>

  </sect2>

  <!-- <sect2 id="network-interface-bonding">
    <title>Network Interface Bonding</title>

    <sect3 role="kernel" id="advanced-network-bonding-kernel">
      <title>Kernel Configuration</title>

      <para>
        Enable the following options in the kernel configuration
        and recompile the kernel if necessary:
      </para>

<screen><literal>Device Drivers:
  Network device support: Y
    Bonding driver support: M or Y</literal></screen>

      <indexterm zone="advanced-network advanced-network-bonding-kernel">
        <primary sortas="d-advanced-network-bonding">Advanced Network Bonding</primary>
      </indexterm>

    </sect3>

    <sect3>
      <title>Setting up Network Interface Bonding</title>

    </sect3>

  </sect2> -->

</sect1>
