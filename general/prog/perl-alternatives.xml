<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;

  <!-- it seems at least one entity is needed -->
  <!ENTITY perl-alternatives-dummy      "42">
]>
  <!-- Begin perl alternatives -->
  <sect2 id="perl-alternatives" xreflabel="CPAN 自动安装 Perl 模块">

    <title>CPAN 自动安装 Perl 模块</title>

    <indexterm zone="perl-alternatives">
      <primary sortas="a-cpan-install-of-perl-modules">CPAN 自动安装 Perl 模块</primary>
    </indexterm>

    <sect3 role="installation" id="perl-auto-install"
    xreflabel="自动安装 Perl 模块">
      <title>自动安装 Perl 模块</title>

      <para>
        还有一种安装模块的替代方法，即使用 <command>cpan</command> shell 的 <command>install</command> 命令。该命令会自动从 CPAN 档案库下载模块的最新源码，以及上游列出的所有缺失的依赖模块。然后对每个模块进行解包、编译、测试并安装。
      </para>

      <para>
        在运行自动化安装方法之前，你仍然需要安装所有非 Perl 依赖项。安装后你可能希望清理 <filename class="directory">build/</filename> 目录以释放空间。如果提到任何安装后操作（如创建符号链接），你也应该执行这些操作。
      </para>

      <para>
        首次运行 <command>cpan</command> 时，系统会提示你输入一些关于下载位置和方式的信息。这些信息会保存在 <filename class='directory'>~/.cpan</filename> 目录下的文件中。
      </para>

      <para>
        特别地，你可能希望将其配置为使用 <xref linkend="sudo"/> 进行安装，以便以普通用户身份构建和测试。以下示例未采用这种方式。
      </para>

      <para>
        以 <systemitem class="username">root</systemitem> 用户身份执行 '<command>cpan</command>' 命令启动 <command>cpan</command> shell。现在可以在 <emphasis role='underlined'><prompt>cpan&gt;&nbsp;</prompt></emphasis> 提示符下使用以下命令安装任意模块：
      </para>

<screen role="root"><userinput>install <replaceable>&lt;Module::Name&gt;</replaceable></userinput></screen>

      <para>
        如需其他命令和帮助，请在 <emphasis role='underlined'><prompt>cpan&gt;&nbsp;</prompt></emphasis> 提示符下执行 '<command>help</command>'。
      </para>

      <para>
        另外，对于脚本化或非交互式安装，可以以 <systemitem class="username">root</systemitem> 用户身份使用以下语法安装一个或多个模块：
      </para>

<screen role="root"><userinput>cpan -i <replaceable>&lt;Module1::Name&gt; &lt;Module2::Name&gt;</replaceable></userinput></screen>

      <para>
        查看 <filename>cpan.1</filename> 手册页，了解可在命令行中传递给 <command>cpan</command> 的其他参数。
      </para>

    </sect3>

  </sect2>
