<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN"
   "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
  <!ENTITY % general-entities SYSTEM "../../general.ent">
  %general-entities;
]>

<sect1 id="svnserver" xreflabel="运行 Subversion 服务器">
  <?dbhtml filename="svnserver.html"?>


  <title>运行 Subversion 服务器</title>

  <sect2 role="package">
    <title>运行 Subversion 服务器</title>

    <para>
      本节将介绍如何设置、管理和保护
      <application>Subversion</application> 服务器。
    </para>

    <bridgehead renderas="sect3">Subversion 服务器依赖</bridgehead>

    <bridgehead renderas="sect4">必需</bridgehead>
    <para role="required">
      <xref linkend="subversion"/> 和
      <xref linkend="openssh"/>
    </para>

  </sect2>

  <sect2 role="configuration">
    <title>设置 Subversion 服务器</title>

    <para>
      以下说明将安装一个
      <application>Subversion</application> 服务器，该服务器将配置为
      使用 <application>OpenSSH</application> 作为安全远程访问方法，并提供
      <command>svnserve</command> 用于匿名访问。
    </para>

    <para>
      <application>Subversion</application> 服务器的配置包括以下步骤：
    </para>

    <sect3>
      <title>1. 设置用户、组和权限</title>

      <para>
        配置的初始部分需要以
        <systemitem class='username'>root</systemitem> 用户身份执行。使用以下命令创建
        <systemitem class="username">svn</systemitem> 用户和组：
      </para>

<screen role="root"><userinput>groupadd -g 56 svn &amp;&amp;
useradd -c "SVN Owner" -d /home/svn -m -g svn -s /bin/false -u 56 svn</userinput></screen>

      <para>
        如果计划拥有多个仓库，应该为每个仓库设置一个专用组以方便管理。使用以下
        命令为测试仓库创建 <systemitem class="groupname">svntest</systemitem> 组，并将
        <systemitem class="username">svn</systemitem> 用户添加到该组：
      </para>

<screen role="root"><userinput>groupadd -g 57 svntest &amp;&amp;
usermod -G svntest -a svn</userinput></screen>

      <para>
        此外，在处理仓库时应设置 <command>umask 002</command>，以便所有新建文件
        都可被所有者和组写入。可以通过为 <command>svn</command> 和
        <command>svnserve</command> 创建包装脚本来强制实现这一点：
      </para>

<screen role="root"><userinput>mv /usr/bin/svn /usr/bin/svn.orig &amp;&amp;
mv /usr/bin/svnserve /usr/bin/svnserve.orig &amp;&amp;
cat &gt;&gt; /usr/bin/svn &lt;&lt; "EOF"
<literal>#!/bin/sh
umask 002
/usr/bin/svn.orig "$@"</literal>
EOF
cat &gt;&gt; /usr/bin/svnserve &lt;&lt; "EOF"
<literal>#!/bin/sh
umask 002
/usr/bin/svnserve.orig "$@"</literal>
EOF
chmod 0755 /usr/bin/svn{,serve}</userinput></screen>

      <note>
        <para>
          如果使用 <application>Apache</application> 通过 HTTP 访问仓库，
          即使是匿名访问，也应该用类似的脚本包装
          <command>/usr/sbin/httpd</command>。
        </para>
      </note>

    </sect3>

    <sect3>
      <title>2. 创建 Subversion 仓库</title>

      <para>
        有几种方法可以设置 Subversion 仓库。建议查看 <ulink
        url="https://svnbook.red-bean.com/nightly/en/svn.reposadmin.html">《SVN
        Book》</ulink> 的相应章节。可以使用以下说明设置一个基本仓库。
      </para>

      <para>
        使用以下命令（以 <systemitem class="username">root
        </systemitem> 用户身份）创建一个新的 <application>Subversion</application> 仓库：
      </para>

<screen role="root"><userinput>install -v -m 0755 -d /srv/svn &amp;&amp;
install -v -m 0755 -o svn -g svn -d /srv/svn/repositories &amp;&amp;
svnadmin create /srv/svn/repositories/svntest</userinput></screen>

      <para>
        仓库创建完成后，应该填充一些有用的内容。你需要预先设置好
        目录结构，确保它完全按照你希望仓库的样子来组织。例如，
        这里是一个以 <filename>svntest/</filename> 为根的示例 BLFS 布局设置。
        你需要设置类似于以下的目录树：
      </para>

<screen><literal>svntest/            # 仓库名称
   trunk/           # 包含现有的源代码树
      BOOK/
      bootscripts/
      edguide/
      patches/
      scripts/
   branches/        # 额外分支所需
   tags/            # 标记发布点所需</literal></screen>

      <para>
        按照上述内容创建目录布局后，就可以进行初始导入：
      </para>

<screen role="root"><userinput>svn import -m "Initial import." \
    <replaceable>&lt;/path/to/source/tree&gt;</replaceable>      \
    file:///srv/svn/repositories/svntest</userinput></screen>

      <para>
        现在修改仓库的所有者和组信息，并将一个非特权用户添加到
        <systemitem class="groupname">svn</systemitem> 和
        <systemitem class="groupname">svntest</systemitem> 组：
      </para>

<screen role="root"><userinput>chown -R svn:svntest /srv/svn/repositories/svntest    &amp;&amp;
chmod -R g+w         /srv/svn/repositories/svntest    &amp;&amp;
chmod g+s            /srv/svn/repositories/svntest/db &amp;&amp;
usermod -G svn,svntest -a <replaceable>&lt;username&gt;</replaceable></userinput></screen>

      <para>
        <systemitem class="groupname">svntest</systemitem> 是分配给
        svntest 仓库的组。如前所述，这在使用 <application>OpenSSH</application> 进行认证时，
        可以简化多个仓库的管理。接下来，你需要将非特权用户以及任何希望获得
        仓库写入权限的其他用户添加到 <systemitem class="groupname">svn</systemitem> 和
        <systemitem class="groupname">svntest</systemitem> 组。
      </para>

      <para>
        此外，你会注意到新仓库的 <filename>db</filename> 目录设置了 set-groupID
        位。如果原因不显而易见，当使用任何外部认证方法（例如
        <command>ssh</command>）时，设置粘滞位是为了使所有新文件的所有者都是该用户，
        但组为 <systemitem class="groupname">svntest</systemitem>。
        <systemitem class="groupname">svntest</systemitem> 组中的任何人都可以创建文件，
        同时仍然给予整个组对这些文件的写入权限。这可以避免将其他用户锁定在仓库之外。
      </para>

      <para>
        现在，返回到非特权用户帐户，使用 <command>svnlook</command> 查看新仓库：
      </para>

<screen><userinput>svnlook tree /srv/svn/repositories/svntest/</userinput></screen>

      <note>
        <para>
          可能需要注销并重新登录以刷新组成员资格。<command>su
          <replaceable>&lt;username&gt;</replaceable></command>
          也应该有效。
        </para>
      </note>

    </sect3>

    <sect3>
      <title>3. 配置服务器</title>

      <para>
        如前所述，这些说明将配置服务器仅使用 <command>ssh</command> 对仓库进行
        写入访问，并使用 <command>svnserve</command> 提供匿名访问。还有几种
        其他方法可以提供对仓库的访问。这些额外配置的最佳说明请参阅
        <ulink url="https://svnbook.red-bean.com/"/>。
      </para>

      <para>
        需要为每个仓库进行访问配置。使用以下命令为 svntest 仓库创建
        <filename>svnserve.conf</filename> 文件：
      </para>

<screen role="root"><userinput>cp /srv/svn/repositories/svntest/conf/svnserve.conf \
   /srv/svn/repositories/svntest/conf/svnserve.conf.default &amp;&amp;

cat &gt; /srv/svn/repositories/svntest/conf/svnserve.conf &lt;&lt; "EOF"
<literal>[general]
anon-access = read
auth-access = write</literal>
EOF</userinput></screen>

      <para>
        配置文件的内容其实不多。你会注意到只需要 general 段。
        查看 <filename>svnserve.conf.default</filename> 文件以获取有关使用
        <command>svnserve</command> 内置认证方法的信息。
      </para>

    </sect3>

    <sect3 id="svnserver-init">
      <title>4. 启动服务器</title>

      <para revision="sysv">
        要在启动时启动服务器，请安装包含在 <xref linkend="bootscripts"/> 软件包中的 svn
        启动脚本：
      </para>

      <para revision="systemd">
        要在启动时启动服务器，请从
        <xref linkend="systemd-units"/> 软件包安装
        <filename>svnserve.service</filename> 单元：
      </para>

      <indexterm zone="svnserver svnserver-init" revision="sysv">
        <primary sortas="f-svn">svn</primary>
      </indexterm>

<screen role="root" revision="sysv"><userinput>make install-svn</userinput></screen>

      <indexterm zone="svnserver svnserver-init" revision="systemd">
        <primary sortas="f-svnserve">svnserve</primary>
      </indexterm>

<screen role="root" revision="systemd"><userinput>make install-svnserve</userinput></screen>

      <para revision="systemd">
        此外，上述说明要求 svn 服务器使用 <command>umask 002</command>，以便所有新建文件
        都可被所有者和组写入。可以通过运行以下命令创建 systemd 单元覆盖文件来实现这一点：
      </para>

<screen role="root" revision="systemd"><userinput>mkdir -p /etc/systemd/system/svnserve.service.d
echo "UMask=0002" > /etc/systemd/system/svnserve.service.d/99-user.conf</userinput></screen>

      <para revision="systemd">
        传递给 <command>svnserve</command> 守护进程的选项可以在
        <filename>/etc/default/svnserve</filename> 中更改。
      </para>

    </sect3>

  </sect2>

</sect1>
